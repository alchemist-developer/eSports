import Swiper from 'https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.esm.browser.min.js'

new Swiper('.swiper', {
    // Optional parameters
   slidesPerView: 3,
   loop: true,
    spaceBetween: 5,
    });

